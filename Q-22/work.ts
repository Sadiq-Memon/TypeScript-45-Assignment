// Intentional Error: If you haven’t received an array index error in one of your programs yet, try to make one happen. Change an index in one of your programs to produce an index error. Make sure you correct the error before closing the program.

let indexError = ["Sir Muhammad Bilal", "Sir Ali Aftab Sheikh", '"Ubaid ur Rehman"'];

// If we try to access index 3 of "indexError", which is outside the bounds of the array, an error will occur.
 console.log(indexError[3]); //This line will produce  an error.

 
console.log(indexError);
